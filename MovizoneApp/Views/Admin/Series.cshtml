@model List<MovizoneApp.Models.TVSeries>
@{
    ViewData["Title"] = "TV Series Management";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>TV Series</h2>
    <a asp-action="CreateSeries" class="btn btn-primary">
        <i class="ti ti-plus"></i> Add New Series
    </a>
</div>

<!-- Search Form -->
<div class="card border-0 shadow-sm mb-3">
    <div class="card-body">
        <form asp-action="Series" method="get" class="row g-3">
            <div class="col-md-10">
                <input type="text" name="search" class="form-control" placeholder="Search series by title or description..." value="@ViewBag.SearchQuery">
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-primary w-100">
                    <i class="ti ti-search"></i> Search
                </button>
            </div>
        </form>
    </div>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Year</th>
                        <th>Rating</th>
                        <th>Seasons</th>
                        <th>Episodes</th>
                        <th>Status</th>
                        <th>Featured</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var series in Model)
                    {
                        <tr>
                            <td>@series.Id</td>
                            <td>
                                <strong>@series.Title</strong><br>
                                <small class="text-muted">@series.Creator</small>
                            </td>
                            <td>@series.Year</td>
                            <td>
                                <span class="badge bg-success">@series.Rating</span>
                            </td>
                            <td>@series.Seasons</td>
                            <td>@series.TotalEpisodes</td>
                            <td>
                                @if (series.Status == "Ongoing")
                                {
                                    <span class="badge bg-info">@series.Status</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">@series.Status</span>
                                }
                            </td>
                            <td>
                                @if (series.IsFeatured)
                                {
                                    <span class="badge bg-primary">Yes</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">No</span>
                                }
                            </td>
                            <td>
                                <a asp-action="EditSeries" asp-route-id="@series.Id" class="btn btn-sm btn-outline-primary">
                                    <i class="ti ti-edit"></i>
                                </a>
                                <form asp-action="DeleteSeries" asp-route-id="@series.Id" method="post" class="d-inline">
                                    <button type="submit" class="btn btn-sm btn-outline-danger"
                                            onclick="return confirm('Delete this series?')">
                                        <i class="ti ti-trash"></i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination -->
        @{
            var currentPage = ViewBag.CurrentPage ?? 1;
            var totalPages = ViewBag.TotalPages ?? 1;
            var searchQuery = ViewBag.SearchQuery ?? "";
        }
        @if (totalPages > 1)
        {
            <nav aria-label="Page navigation" class="mt-4">
                <ul class="pagination justify-content-center">
                    <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                        <a class="page-link" asp-action="Series" asp-route-page="@(currentPage - 1)" asp-route-search="@searchQuery">
                            <i class="ti ti-chevron-left"></i> Previous
                        </a>
                    </li>

                    @for (int i = 1; i <= totalPages; i++)
                    {
                        <li class="page-item @(i == currentPage ? "active" : "")">
                            <a class="page-link" asp-action="Series" asp-route-page="@i" asp-route-search="@searchQuery">@i</a>
                        </li>
                    }

                    <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                        <a class="page-link" asp-action="Series" asp-route-page="@(currentPage + 1)" asp-route-search="@searchQuery">
                            Next <i class="ti ti-chevron-right"></i>
                        </a>
                    </li>
                </ul>
            </nav>
        }
    </div>
</div>
